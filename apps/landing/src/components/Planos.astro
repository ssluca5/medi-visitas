---
const appUrl = import.meta.env.PUBLIC_APP_URL || 'https://app.medivisitas.com';

const planos = [
  {
    nome: "Básico",
    slug: "BASICO",
    preco: "R$ 79",
    desc: "Para organizar a carteira e manter a rotina comercial em dia.",
    popular: false,
    features: [
      "Até 100 profissionais",
      "Agenda inteligente",
      "Histórico de visitas",
      "Pipeline comercial",
      "Notificações automáticas",
      "Suporte em 48h"
    ]
  },
  {
    nome: "Profissional",
    slug: "PROFISSIONAL",
    preco: "R$ 149",
    desc: "Para representantes que usam IA, relatórios e metas na rotina.",
    popular: true,
    features: [
      "Profissionais ilimitados",
      "Metas de visitas e pipeline",
      "50 transcrições de IA/mês",
      "Pacotes adicionais de IA",
      "Relatórios e CSV",
      "Tudo do Básico",
      "Suporte em 24h"
    ]
  },
  {
    nome: "Equipe",
    slug: "EQUIPE",
    preco: "R$ 349",
    desc: "Para gestores com até 10 propagandistas e distribuição de metas.",
    popular: false,
    features: [
      "Até 10 usuários",
      "Metas distribuídas pelo gestor",
      "200 transcrições compartilhadas",
      "Dashboard do gestor",
      "Uso de IA por membro",
      "Tudo do Profissional",
      "Suporte prioritário em 4h"
    ]
  }
];

const comparativo = [
  ["Profissionais", "100", "Ilimitado", "Ilimitado"],
  ["Transcrições IA", "-", "50/mês", "200/mês"],
  ["Metas", "-", "Individual", "Distribuídas pelo gestor"],
  ["Relatórios CSV", "-", "Sim", "Sim"],
  ["Gestão de equipe", "-", "-", "Até 10"],
  ["Suporte", "48h", "24h", "4h"]
];
---

<section class="section-plans" id="planos" aria-labelledby="plans-h2">
  <div class="plans-inner">
    <span class="pill pill-blue story-element" data-story-delay="0">Planos</span>
    <h2 class="plans-h2 story-element" id="plans-h2" data-story-delay="1">Planos para cada fase da operação.</h2>

    <div class="plans-grid">
      {planos.map((plano, index) => (
        <article class={`plan-card ${plano.popular ? "plan-popular" : ""} story-element`} data-story-delay={index + 1}>
          {plano.popular && <div class="popular-badge">Mais popular</div>}
          <div class="plan-name">{plano.nome}</div>
          <div class="plan-price">
            <span>{plano.preco}</span><span class="plan-price-period">/mês</span>
          </div>
          <p class="plan-desc">{plano.desc}</p>
          <ul class="plan-list">
            {plano.features.map((feature) => <li>{feature}</li>)}
          </ul>
          <a href={`${appUrl}/signup?plano=${plano.slug}`} class="plan-cta">Assinar →</a>
        </article>
      ))}
    </div>

    <div class="enterprise-card story-element" data-story-delay="4">
      <div>
        <div class="plan-name">Empresarial</div>
        <h3>Mais de 10 usuários</h3>
        <p>Preço, onboarding e funcionalidades customizados para times maiores.</p>
      </div>
      <a href="/contato" class="enterprise-cta">Falar com o comercial</a>
    </div>

    <div class="comparison story-element" data-story-delay="5" aria-label="Tabela comparativa de planos">
      <div class="comparison-row comparison-head">
        <span>Funcionalidade</span>
        <span>Básico</span>
        <span>Profissional</span>
        <span>Equipe</span>
      </div>
      {comparativo.map((row) => (
        <div class="comparison-row">
          {row.map((cell) => <span>{cell}</span>)}
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .section-plans {
    background: var(--color-surface);
    padding: 112px clamp(24px, 5vw, 80px);
  }

  .plans-inner {
    max-width: 1120px;
    margin: 0 auto;
    text-align: center;
  }

  .plans-h2 {
    font-size: clamp(34px, 4vw, 46px);
    font-weight: 700;
    font-family: var(--font-display);
    color: var(--color-ink);
    letter-spacing: -0.02em;
    margin-top: 16px;
  }

  .plans-sub {
    font-size: 18px;
    color: var(--color-ink-secondary);
    margin-top: 12px;
    line-height: 1.5;
  }

  .plans-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 20px;
    margin-top: 44px;
    text-align: left;
  }

  .plan-card,
  .enterprise-card,
  .comparison {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 16px;
  }

  .plan-card {
    position: relative;
    padding: 28px;
    display: flex;
    flex-direction: column;
  }

  .plan-popular {
    border-color: var(--color-blue);
    box-shadow: 0 14px 36px color-mix(in srgb, var(--color-blue) 12%, transparent);
  }

  .popular-badge {
    position: absolute;
    right: 20px;
    top: 20px;
    border-radius: 999px;
    background: color-mix(in srgb, var(--color-blue) 10%, transparent);
    color: var(--color-blue);
    font-size: 12px;
    font-weight: 700;
    padding: 6px 10px;
  }

  .plan-name {
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-ink-secondary);
  }

  .plan-price {
    font-size: 42px;
    font-weight: 800;
    color: var(--color-ink);
    margin-top: 18px;
    line-height: 1;
  }

  .plan-price-period {
    font-size: 15px;
    font-weight: 500;
    color: var(--color-ink-muted);
    margin-left: 4px;
  }

  .plan-desc {
    min-height: 44px;
    font-size: 14px;
    color: var(--color-ink-secondary);
    margin-top: 12px;
    line-height: 1.55;
  }

  .plan-list {
    list-style: none;
    margin: 24px 0;
    padding: 0;
    flex: 1;
  }

  .plan-list li {
    font-size: 14px;
    color: var(--color-ink);
    padding: 8px 0;
    display: flex;
    gap: 10px;
  }

  .plan-list li::before {
    content: "✓";
    color: var(--color-blue);
    font-weight: 800;
  }

  .plan-cta,
  .enterprise-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    border-radius: 12px;
    font-size: 14px;
    font-weight: 700;
    text-decoration: none;
    transition: transform 180ms, background 180ms;
  }

  .plan-cta {
    width: 100%;
    background: var(--color-blue);
    color: var(--color-surface);
  }

  .plan-cta:hover,
  .enterprise-cta:hover {
    transform: translateY(-2px);
  }

  .enterprise-card {
    margin-top: 20px;
    padding: 28px 30px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    text-align: left;
    background: var(--color-off-white);
  }

  .enterprise-card h3 {
    color: var(--color-ink);
    font-size: 22px;
    margin-top: 8px;
  }

  .enterprise-card p {
    color: var(--color-ink-secondary);
    margin-top: 6px;
  }

  .enterprise-cta {
    padding: 0 22px;
    color: var(--color-surface);
    background: var(--color-ink);
    white-space: nowrap;
  }

  .comparison {
    margin-top: 32px;
    overflow: hidden;
    text-align: left;
  }

  .comparison-row {
    display: grid;
    grid-template-columns: 1.4fr repeat(3, 1fr);
    border-top: 1px solid var(--color-border);
  }

  .comparison-row:first-child {
    border-top: 0;
  }

  .comparison-row span {
    padding: 14px 16px;
    color: var(--color-ink-secondary);
    font-size: 14px;
  }

  .comparison-row span:not(:first-child) {
    text-align: center;
    color: var(--color-ink);
    font-weight: 600;
  }

  .comparison-head {
    background: var(--color-off-white);
  }

  .comparison-head span {
    color: var(--color-ink);
    font-size: 12px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  @media (max-width: 960px) {
    .plans-grid {
      grid-template-columns: 1fr;
    }

    .enterprise-card {
      align-items: flex-start;
      flex-direction: column;
    }

    .enterprise-cta {
      width: 100%;
    }
  }

  @media (max-width: 640px) {
    .comparison {
      overflow-x: auto;
    }

    .comparison-row {
      min-width: 620px;
    }
  }

  @keyframes slideUpFade {
    0% {
      opacity: 0;
      transform: translateY(24px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .story-element {
    opacity: 0;
    animation: slideUpFade 0.6s cubic-bezier(0.25, 0.1, 0.25, 1) forwards;
  }

  .story-element[data-story-delay="0"] { animation-delay: 0s; }
  .story-element[data-story-delay="1"] { animation-delay: 0.1s; }
  .story-element[data-story-delay="2"] { animation-delay: 0.2s; }
  .story-element[data-story-delay="3"] { animation-delay: 0.3s; }
  .story-element[data-story-delay="4"] { animation-delay: 0.4s; }
  .story-element[data-story-delay="5"] { animation-delay: 0.5s; }
</style>
